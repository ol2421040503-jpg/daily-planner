# 每日规划 v1.6.0 发布说明

## 发布日期
2026-03-24

## 重要更新

### 代码架构重构
- **模块化拆分**：将 8000+ 行的 main.ts 拆分成多个独立模块
- **类型统一管理**：创建 `src/types/index.ts` 统一类型定义
- **配置集中管理**：创建 `src/config/index.ts` 管理常量配置
- **工具函数提取**：创建 `src/utils/` 目录存放通用工具函数
  - `date.ts`：日期格式化、农历转换等工具
  - `image.ts`：图片压缩、剪贴板读取等工具
  - `task.ts`：任务排序、统计计算等工具

### 项目结构优化
```
src/
├── config/          # 配置常量
├── types/           # 类型定义
├── utils/           # 工具函数
├── services/        # 服务层
└── main.ts          # 主应用逻辑
```

## 更新内容

### 知识库优化（v1.5.9）
- 修复搜索闪烁问题：修复个人知识库搜索框输入时页面闪烁的问题
- 搜索时只局部更新指南列表，不再重新渲染整个页面

## 技术改进

| 改进项 | 说明 |
|--------|------|
| 类型安全 | 所有类型统一管理，避免重复定义 |
| 可维护性 | 按功能拆分，职责清晰 |
| 可复用性 | 工具函数可在其他模块复用 |
| 构建优化 | TypeScript 编译通过，构建成功 |

## 系统要求

| 系统 | 支持 |
|------|------|
| Windows 7 SP1 | ✅ 支持 |
| Windows 8/8.1 | ✅ 支持 |
| Windows 10 | ✅ 支持 |
| Windows 11 | ✅ 支持 |

## 本地构建

```bash
# Windows 环境执行
pnpm install
pnpm run electron:build
```

### 输出文件
- `dist-electron/daily-planner-setup-1.6.0.exe`

---
*此版本由 Vibe Coding 自动生成*
